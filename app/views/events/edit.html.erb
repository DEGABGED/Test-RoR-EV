<%= link_to("Back", group_event_path(@group, @event)) %>
<br>
<br>

<%= simple_form_for [@group, @event] do |e| %>
  <%= e.input :title %>
  <%= e.input :pubmat%>
  <%= e.input :venue %>
  <%= e.input :time %>
  <%= e.input :time_end %>
  <br>
  <%= e.input :desc %>
  <%= e.button :submit %>
<% end %>

