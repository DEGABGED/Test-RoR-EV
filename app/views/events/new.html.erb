<%= simple_form_for [@group, @group.events.build] do |e| %>
  <%= e.input :title %>
  <%= e.input :pubmat%>
  <%= e.input :venue %>
  <%= e.input :time %>
  <%= e.input :time_end %>
  <br>
  <%= e.input :desc %>
  <%= e.button :submit %>
<% end %>
